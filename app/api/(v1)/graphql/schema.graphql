type User {
    id: ID!
    email: String!
    username: String!
    firstname: String!
    lastname: String!
    age: Int!
    createdAt: String
    updatedAt: String
    deletedAt: String
}

type Query {
    ping: String!
    users: [User!]!
    user(id: ID!): User
}
